# Database — provided by Neon/Vercel integration
DATABASE_URL=

# Admin authentication — keep values in Vercel Environment Variables only
ADMIN_EMAIL=
ADMIN_PASSWORD=
ADMIN_SETUP_ENABLED=false
# Temporary password-only admin login mode; keep false after rotating credentials.
ADMIN_PASSWORD_ONLY_MODE=false
JWT_SECRET=

